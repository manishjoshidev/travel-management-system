type Courier {
  id: ID!
  status: String
  user: User
}

type Query {
  couriers(page: Int!, size: Int!): [Courier]
}

type Mutation {
  updateCourierStatus(courierId: ID!, status: String!): Courier
}
